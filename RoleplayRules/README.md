# Server Response Rules

Each server has its own `roleplay_rules.txt`. Role Weaver automatically adds the selected server's rules to the LLM instructions for generated replies and candidate replies.

Edit the file under `RoleplayRules/<server>/roleplay_rules.txt` when a server needs different formatting or roleplay constraints.
